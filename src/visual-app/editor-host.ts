// Moved to src/host/editor-host.ts (ADR 0156); re-exported so the editor's
// imports and the published `yarramate/visual-app` names stand.
export type { EditorHost, EditorHostEvents } from '../host/editor-host.js'
