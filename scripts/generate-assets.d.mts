/** The shipped question catalogue as a TypeScript module. */
export declare const generateShippedCatalogue: () => string
/** The LikeC4 specification as a TypeScript module. */
export declare const generateLikeC4Specification: () => string
/** Every module the script writes, with its generator. */
export declare const OUTPUTS: readonly (readonly [string, () => string])[]
