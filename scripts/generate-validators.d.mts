/** The standalone validator module for the nine shared-option schemas. */
export declare const generate: () => string
/** The `discriminator` half, emitted from its own Ajv instance. */
export declare const generateOperations: () => string
